[project]
name = "boukensha"
version = "0.0.0"
requires-python = ">=3.12"
dependencies = [
    "python-dotenv",
    "pyyaml",
    "textual",
    "opentelemetry-api",
    "opentelemetry-sdk",
    "opentelemetry-exporter-otlp-proto-http",
]

[tool.uv]
package = false
